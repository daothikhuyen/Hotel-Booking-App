import 'package:flutter/material.dart';

class PageAlterNull extends StatelessWidget {
  const PageAlterNull({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    body: Center(child: Text('No hotel data')),
  );
  }
}
